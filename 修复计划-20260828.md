# 修复计划（2026-08-28）

> 来源：`整体审查.md` 逐条核实 + 实际代码验证。
> **S1/S2/L4 已于昨晚修复完毕**（邮件转义 + 专辑列表 artists + 批量投稿裸键，语法/类型检查通过），见文末「已完成」。
> 本文档是**剩余全部待改项**，按批次组织，醒来后按顺序执行即可。

---

## 批次 1：M3 + M5（顺手修，两处小改动）

### M3 艺术家 ID 冲突预检

**现状**：三处新建艺术家入口都只做非空校验，撞已有 ID 直接抛 PK 冲突原生错误，不友好：

1. `SongsView.vue` 录歌页内联表单（[ArtistInlineForm.vue:99-104](src/components/admin/ArtistInlineForm.vue)）
2. `SubmissionsView.vue` 审核发布链（[943-954 行](src/views/admin/SubmissionsView.vue)，missing 校验之后无查重）
3. `ArtistsView.vue` 主列表新增（走自动生成 ID `'a'+Date.now()+random`，[362 行](src/views/admin/ArtistsView.vue)，基本不会撞但一并加）

**改法**（三处统一模式）：

```typescript
// 插入前查重（artists 池已在内存中的直接查本地，否则查库）
const exists = artists.value.find(a => a.id === id) // 或 adminApi.getAll('artists', { id: `eq.${id}` })
if (exists) {
  ElMessage.warning(`ID「${id}」已被艺术家「${exists.name}」占用，请换一个`)
  return
}
```

- **ArtistInlineForm.save()**：`isNew` 分支填完 ID 后、写回 tag 前查内存池（父组件传入或 emit 校验）——最简：组件内 `import { useArtistsStore }` 不可行（无 store），改为 **props 传 artists 列表** 或在 emit 后由父组件校验。**推荐：组件内直接 adminApi.getAll('artists', { id: `eq.${id}`, select: 'id,name' }) 查库**（一次请求，简单可靠）
- **SubmissionsView.publishSubmission**：missing 校验后、insert 前，对 `newList` 逐个查本地 `artists.value`（发布链运行时池已加载），命中同 ID 不同名 → 报 `'missing'` 风格错误并提示换 ID
- **ArtistsView.save**：自动 ID 无需预检（时间戳+随机），跳过

### M5 html404 Host 转义

**现状**：[open-api.js:85-110](cloudflare/open-api.js) `html404(host)` 把 Host 头未转义插进 HTML（106 行 `${host}`），反射型注入（低危）。

**改法**：Worker 顶部加转义工具（或复用已有）：

```javascript
const escapeHtml = s => String(s).replace(/[&<>"']/g, c =>
  ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]))
```

`html404` 内 `${host}` → `${escapeHtml(host)}`。

**部署**：改了 open-api.js → **Worker 重部署**。

---

## 批次 2：M1 发布链补偿回滚（单独回归，别跟批次 1 混）

**现状**：[SubmissionsView.vue:956-1144](src/views/admin/SubmissionsView.vue) `publishSubmission` 先置 `status=approved`（958 行）再顺序建艺术家/贡献者/专辑/歌曲/中间表，任一步失败（catch 在 1139 行）投稿卡在「已通过」但产物残缺，审核员看到 ElMessage 报错但状态已 approved，误以为成功。

**改法**（方案 A 最小补偿，不动架构）：

1. **catch 块（1139-1143 行）加回滚**：失败时回写 `status='pending'` + 清 `approved_at`，让投稿回到待审核列表可重试：

```typescript
} catch (e: any) {
  // 补偿回滚：把第一步误置的 approved 拉回 pending，避免"假通过"残留
  adminApi.update('submissions', sub.id, { status: 'pending', approved_at: null, published_refs: null })
    .catch(e2 => console.warn('[补偿回滚失败]', sub.song_data?.title, e2?.message))
  if (!silent) ElMessage.error('发布失败已回滚到待审核：' + e.message)
  else console.warn('[批量发布失败-已回滚]', sub.song_data?.title, e.message)
  return 'error'
}
```

   注意：
   - `published_refs: null` 必须清——半途 insert 的产物（可能已建了艺术家）没有 refs 记录会变孤儿；清掉后 refs 只在完整成功时写入。**残留的部分产物**（比如艺术家建了、歌没建成）：接受——这正是撤回/级联回收链路存在的原因，且 FK RESTRICT 保证不会产生脏引用。极端情况人工清理（已有孤儿清理 SQL 模式，见 phase3 文档执行记录）
   - 回滚本身用 fire-and-forget（`.catch` 吞掉）——回滚失败不能掩盖原始错误

2. **published_refs 写入时机不变**（1130 行，成功尾部）——它就是"完整成功"的标记，别动。

**回归项**（改完必须测）：
- 正常单笔通过 → 全链路成功，状态 approved + refs 齐全（原有行为不变）
- 人为制造失败（最简单：审核表单里新建艺术家填一个**已存在的 ID**——批次 1 的 M3 会让它提前拦住，那就临时断网测试）→ 投稿回到待审核，列表刷新可见
- 批量按标记提交（部分失败场景）→ 失败行回 pending、成功行正常，汇总弹窗数字对得上

---

## 批次 3：L 类清理（不影响正确性，可推后）

### L3 赞助头像字段悬空

**现状**：[SponsorsView.vue](src/views/admin/SponsorsView.vue) 表单有头像输入（105 行 `form.avatar`）、模板展示 `row.avatar`，但 `save()` payload（207-215 行）**不含 avatar** → 保存头像无效。`Sponsor` 类型（[types.ts:218](src/lib/types.ts)）也无此字段。

**改法**：payload 加 `avatar: form.avatar.trim() || null`；`Sponsor` 接口加 `avatar?: string | null`。（前提：确认 sponsors 表有 avatar 列——查 `SELECT column_name FROM information_schema.columns WHERE table_name='sponsors'`；没有就先 `ALTER TABLE sponsors ADD COLUMN IF NOT EXISTS avatar text;`）

### L5 死代码清理

**现状**：
- `legacyCommentsApi`（[api.ts:563](src/lib/api.ts)）：全库无引用（已 grep 验证），注释自己都说"仅当 Phase 3 确认"——Phase 3 都做完了
- `Comment` 接口（[types.ts:237-247](src/lib/types.ts)）：仅被 legacyCommentsApi 引用
- `markReject` 空 if 块（审查提到，位置待查——grep `markReject` 确认后删）

**改法**：三个一起删，跑 `npx vue-tsc --noEmit` 确认无引用报错。

### L1 常量重复（低优先级，纯提纯）

Logo URL 硬编码 4 处、`GENRE_OPTIONS`/`TIP_ICONS`/`langColor`/`copyText`/`VALIDATION_ABORT`/二维码弹窗各 2-3 份。**建议**：单独一次会话做，改动面广需逐处比对；本次跳过也完全没问题。

### L2 性能（低优先级）

- `SubmissionsView` `displayList` O(n·m) 重复 filter（597/624 行）
- `recallSubmissions` 每行串行 5-6 次查询
- `SettingsView.save` 串行 6 upsert

500 首歌 / 几十投稿规模下无感，**观察即可，不改**。

### M2 trgm 索引（低优先级）

6f 后 RPC 已从 ilike 换 strpos，trgm 索引用不上；strpos 全扫当前规模毫秒级。**不改**，数据量上万再议（届时方案：换回 ilike + trgm GIN，或上全文检索）。

### M4 文档版本标注（低优先级）

给 `sql/` 下历史 RPC 文档头部加「已废弃，线上权威版本见 phase3-song-secrets.md 6f」标注。**简单做法**：哪天顺手改，或不管（phase 文档头都有执行记录，翻不到哪里去）。

---

## 部署清单（全部批次做完后）

| 目标 | 方式 | 覆盖改动 |
|---|---|---|
| 主站 | push 触发 Pages/GH Actions | M3（三处查重）、M1（补偿回滚）、L3、L5 |
| Worker | CF Dashboard 手动粘贴重部署 | M5（html404 转义）+ **昨晚的 S2** |
| Netlify | 自动（push 即部署 Functions） | **昨晚的 S1**（已含在上次 push 里的话跳过） |

## 验证清单（部署后过一遍）

1. `/v1/albums` items 的 artists 数组有名字（S2 回归）
2. 投一笔测试投稿（昵称带 `<b>test</b>`）→ 站长收到的邮件里昵称显示为转义文本（S1 回归）
3. 批量投稿 → 查 submissions.song_data 无 `artist/lyricist` 裸键（L4 回归）
4. 后台录歌新建艺术家填已有 ID → 友好提示而非原生报错（M3）
5. 访问 `https://api.lrcshare.com/不存在的子域路径` 404 页正常（M5）
6. 审核一笔投稿中途制造失败（断网）→ 投稿回到待审核（M1）
7. 赞助编辑填头像 → 保存 → 重新打开有值（L3，需先确认表有列）
8. `npx vue-tsc --noEmit` 通过（L5 删码后）

---

## 已完成（昨晚，无需再做）

| # | 改动 | 文件 |
|---|---|---|
| S1 | generateEmailHTML 三字段 escapeHtml + 邮件主题控制字符剥离（审查没发现的信头注入） | [mailer.mjs:58-67, 198-226](netlify/functions/mailer.mjs) |
| S2 | handleAlbums `a.artist_ids` → `albumArtistIdsOf(a)` | [open-api.js:557](cloudflare/open-api.js) |
| L4 | 批量投稿 song_data 删 5 个拼接裸键输出 | [BatchSubmitPanel.vue:397-404](src/components/submit/BatchSubmitPanel.vue) |

语法检查（node --check）+ 类型检查（vue-tsc）均已通过。
