# LrcShare V3 整体审查（问题清单复核版）

> 初版审查：2026-08-27。
> **复核：2026-08-28**——逐条对照代码核实，标注修复状态。
> 复核说明：初版审查基于阶段三完成前的代码快照，部分问题在审查期间已被顺手修复（如 S3），部分于 2026-08-28 两批集中修复。
> 相关执行记录：`修复计划-20260828.md`

---

## 状态总览

| 结论 | 条目 | 明细 |
| --- | --- | --- |
| ✅ 已修复（13） | S1、S2、S3、M1、M2、M3、M5、L3、L4、L5、M4、L1、L2 | 见分级明细表 |

> 精确计数：**13 项全部修复，无遗留**。初版「不修/暂缓」的 M2、M4、L1、L2 已全部翻案处理。

---

## 二、问题分级（复核后状态）

### 【严重风险】

| # | 问题 | 状态 | 备注 |
| --- | --- | --- | --- |
| **S1** | `mailer.mjs` `generateEmailHTML` 对 `user_name/song_title/reject_reason` 不转义拼进 HTML，恶意投稿者可向站长邮箱注入 HTML/钓鱼链接 | ✅ **已修（2026-08-28）** | 三个字段入口统一 `escapeHtml`（[mailer.mjs:58-67](netlify/functions/mailer.mjs)）；**复核时另发现审查未覆盖的信头注入**：邮件主题也拼 `song_title`，换行符可拆信头——已加 `cleanSubjectText` 剥离控制字符（3 处 subject） |
| **S2** | `open-api.js` `handleAlbums` 用 `a.artist_ids` 取专辑艺术家，但 select 返回的是 `album_contributors` 嵌入 → `/v1/albums` 列表 `artists` 恒为空 | ✅ **已修（2026-08-28）** | 一行：`flatMap(a => a.artist_ids)` → `flatMap(albumArtistIdsOf)`（[open-api.js:561](cloudflare/open-api.js)）。系阶段二批次 4 改造的漏网之鱼（详情/摘要等 3 处都改了，列表漏了） |
| **S3** | `get_artist_songs` RPC 仍读旧列，B 段删列清单遗漏对它的单源化 → 艺术家作品页 500 | ✅ **已修（2026-08-27，阶段三期间）** | 审查出具前已被发现并修复：phase3-song-secrets.md 步骤 1 打包了 `get_artist_songs` 单源重写（只读 `song_contributors`），已执行并验证（art_hotdog）。初版审查写作时 B 段已执行，该问题当时真实存在（艺术家页故障）但已随 phase3 步骤 1 解决 |

### 【一般隐患】

| # | 问题 | 状态 | 备注 |
| --- | --- | --- | --- |
| **M1** | 发布链无事务：先置 `approved` 再顺序建实体，任一步失败留「已通过但产物残缺」中间态 | ✅ **已修（2026-08-28，方案 A + 分流增强）** | catch 按产物分流（[SubmissionsView.vue:1151-1168](src/views/admin/SubmissionsView.vue)）：① 无任何产物 → 拉回 pending，报错「已回滚到待审核」；② 已建部分产物 → **保留 refs、留在已通过 tab**（pending 无撤回按钮，拉回去重审会重复建歌），报错提示「撤回本条回收后重试」，撤回链按 refs 级联回收残缺产物。注意 `refs` 声明已提升到 try 外供 catch 读取。**待回归验证**（见回归清单 6）。方案 B（下沉 SECURITY DEFINER RPC 原子化）仍属长期选项，当前不排期 |
| **M2** | 搜索 `ilike '%kw%'` 全表扫描，无 trgm 索引 | ✅ **已修（2026-08-28）** | 两段式检索落地：[sql/search-recall-two-stage.md](sql/search-recall-two-stage.md)——songs 冗余列 `search_text`（title+aliases+歌曲级/专辑级艺术家及别名，触发器自动同步）+ trgm GIN 表达式索引做召回层，候选集上跑 6f 精确复核与排序（**排序语义一字未动**，结果与改造前逐字节一致）。已执行并验证：结果一致性 ✅、`Bitmap Index Scan` 实测 ✅（`show_trgm('北京')` 非空，CJK 可提词）。已知限制：单字符 token（`爱`/`a`）提不出 trigram，该类查询退化为顺序过滤——结果正确仅慢，罕见可接受 |
| **M3** | 后台新建艺术家 ID 只查非空，撞已有 ID 抛 PK 原生错误 | ✅ **已修（2026-08-28）** | 两处预检：① [ArtistInlineForm.vue:105-114](src/components/admin/ArtistInlineForm.vue) 填 ID 时查库，占用即提示「已被艺术家 xxx 占用」；② [SubmissionsView.vue:954-965](src/views/admin/SubmissionsView.vue) 发布前对本地池校验，同名视为行内绑定放行、异名真冲突才拦。ArtistsView 主列表用自动 ID（时间戳+随机）无冲突面，不加 |
| **M4** | SQL 迁移文档版本漂移：同一 RPC 多个历史版本，无「线上权威版本」标记 | ✅ **已完成（2026-08-28）** | 7 个被取代的历史文档头部全部补废弃标注（search-songs-artist-match / search-songs-hardening / revoke-unlock-code-column / search-structured-query〔部分废弃〕/ fix-get-top-artists-count / recompute-artist-types / verify-hidden-unlock-code），统一指向权威版本，注明「仅作沿革记录勿重跑」 |
| **M5** | `html404` 把 Host 头未转义插进 HTML（反射型注入，低危） | ✅ **已修（2026-08-28）** | Worker 加 `escapeHtml` 工具，`${host}` → `${escapeHtml(host)}`（[open-api.js:84-86, 110](cloudflare/open-api.js)） |

### 【优化建议】

| # | 问题 | 状态 | 备注 |
| --- | --- | --- | --- |
| **L1** | 常量/工具重复（Logo URL 4 处、`GENRE_OPTIONS`/`TIP_ICONS`/`langColor`/`copyText`/`VALIDATION_ABORT`/二维码弹窗各 2-3 份） | ✅ **已完成（2026-08-28）** | 收敛到既有 `lib/constants.ts` + 新建 [lib/clipboard.ts](src/lib/clipboard.ts)：LOGO_URL（AppNavbar 本地副本、SubmitView DEFAULT_LOGO、**SearchOverlay DEFAULT_AVATAR——第三处系复核补漏**，全库 grep 该 URL 片段仅剩 constants.ts 定义一处）、GENRE_OPTIONS（SongsView/SubmissionsView）、TIP_ICONS（SongView/SongsView）、langColor + 色板（RichContentView/RichTextToolbar，前后台语种颜色同源）、copyText（SongView/ContactIcons，取两版并集：安全上下文检查 + 降级）、VALIDATION_ABORT（SubmitView/BatchSubmitPanel）、二维码 URL（SupportView/RewardModal 模板改引 QR_CODES）。`vue-tsc` 通过 |
| **L2** | `displayList` O(n·m) 重复 filter；`recallSubmissions` 每行串行 5-6 查询；`SettingsView.save` 串行 6 upsert | ✅ **已完成（2026-08-28，模式级修复）** | 定性纠正：三处是反模式（循环内重复 filter / N+1 查询 / 串行 IO），与数据量无关。① `displayList` 预分组 Map 一趟 O(n)，pending/非 pending 两分支合一；② `recallSubmissions` 改「批量预读取（约 7 个并行查询）→ 内存判定 → 分相删除（歌→专辑→艺术家→贡献者，跨行共享实体先行删歌避免 FK RESTRICT 误拦）」，adminApi.getAll 补 `in` 过滤；③ `SettingsView.save` 改单请求批量 upsert（`upsertBatch`），顺删无引用的 `upsertSetting`。`vue-tsc` 通过 |
| **L3** | `SponsorsView` 头像字段悬空：表单有输入、payload 不含 avatar、类型无字段 → 保存头像无效 | ✅ **已修（2026-08-28）** | payload 补 `avatar`（[SponsorsView.vue:215](src/views/admin/SponsorsView.vue)）+ `Sponsor` 接口加 `avatar?: string \| null`（types.ts）。**前置未验证**：需确认 sponsors 表有 avatar 列，没有则跑 `ALTER TABLE sponsors ADD COLUMN IF NOT EXISTS avatar text;` |
| **L4** | `BatchSubmitPanel.buildSongData` 输出旧列风格裸键（`artist/lyricist/...` 拼接串） | ✅ **已修（2026-08-28）** | 复核提级了优先级：⑨ 规范化刚清完 song_data，批量投稿继续写裸键 = 持续灌脏数据，防复发价值高。删 5 个裸键输出行（[BatchSubmitPanel.vue:397-404](src/components/submit/BatchSubmitPanel.vue)），song_data 只留数组键，与单曲路径对齐 |
| **L5** | 死代码：`legacyCommentsApi`、`Comment` 类型、`markReject` 空 if 块 | ✅ **已修（2026-08-28）** | 三处全删：`legacyCommentsApi`（全库零引用，grep 验证）、`Comment` 接口（连带 api.ts 的 import）、`markReject` 空 if 块。`vue-tsc` 通过 |

---

## 五、结论复核

初版「必须改 4 项」的最终状态：

| 初版结论 | 复核结果 |
| --- | --- |
| 1. S1 邮件 HTML 注入 | ✅ 已修（含审查未发现的信头注入，一并修） |
| 2. S2 Worker 专辑艺术家恒空 | ✅ 已修 |
| 3. S3 get_artist_songs 单源化 | ✅ 修复早于审查（phase3 步骤 1 顺带解决） |
| 4. M1 发布链中间态 | ✅ 已按方案 A 修（补偿回滚）；方案 B（RPC 原子化）留作长期选项 |

**严重/一般隐患至此全部闭环**：M2 已翻案落地（两段式检索，2026-08-28 执行验证通过）、M4 已补标注。L 类修复了 L3/L4/L5（其中 L4 提级），L1/L2 已完成提纯与批量化改造。

## 部署清单（2026-08-28 两批改动）

| 目标 | 方式 | 覆盖 |
| --- | --- | --- |
| 主站 | push 触发构建 | S1 不涉及 / L4、M3、M1、L3、L5 |
| Worker | CF Dashboard 手动重部署 | S2、M5 + 此前的署名时间戳（99:99.999） |
| Netlify | push 自动部署 Functions | S1 |

## 回归验证清单

1. `/v1/albums` items 的 artists 有名字（S2）
2. 投稿昵称带 `<b>test</b>` → 站长邮件显示转义文本（S1）
3. 批量投稿 → song_data 无裸键（L4）
4. 录歌新建艺术家填已有 ID → 友好提示（M3）
5. 未知子域 404 页正常显示（M5）
6. M1 分流回归（断网测不到补偿——第一步就挂，回滚也发不出去）：
   - 正常通过一笔 → 行为不变
   - DevTools → Network → 屏蔽 `song_contributors` 的 insert 请求 → 审核通过一首**新建了艺术家**的投稿 → 走 `syncSongContributors` 抛错路径：报错含「已建部分产物…撤回本条回收」，投稿留在**已通过** tab
   - 在已通过 tab 撤回该条 → 残缺歌曲/新建艺术家被级联回收，回到待审核
7. 赞助编辑填头像 → 保存 → 重开有值（L3，先确认表有列）
8. `npx vue-tsc --noEmit` 通过（已验证 ✅）
9. **撤回链回归（L2，重构后语义必须实测**——`recallSubmissions` 从串行查询改为「批量预读取 → 内存判定 → 分相删除」，`vue-tsc` 只保证类型不保证撤回语义）：
   - 撤回**单条**已通过投稿 → 歌/新建专辑/新建艺术家/新建贡献者按序回收
   - 撤回**整批**（跨行）→ 不误删、不漏删
   - 重点 case：撤回「多行共享同一新建专辑艺术家」的批次 → `artistStillUsed`/`delAlbumIds` 的排除逻辑生效（仍有他行引用的艺术家/专辑**不**被删，无引用的才删）；删除顺序 歌→专辑→艺术家→贡献者，无 FK RESTRICT 误拦

## 架构层结论（初版意见，复核后仍成立）

- 架构方向正确（SSG+水合、计算下沉 PG、安全分层、双源过渡巧思）
- 结构性妥协（客户端发布链无事务、三套数据组装）受托管形态约束，方案 B 类重构（发布链 RPC 原子化、组装收敛、模块拆分）留作长期选项，当前规模不值得投入
- 「把三段式迁移干净地收尾」——已完成（B 段执行 + S3 补漏 + 旧列备份表一周后清理）

## 遗留待办（未完成事项汇总）

| # | 事项 | 触发条件 / 时机 | 操作 |
| --- | --- | --- | --- |
| 1 | 删除 phase2 备份表 | 一周后（2026-09-03 前后） | `DROP TABLE public._backup_p2_songs_contribs, public._backup_p2_albums_contribs;` |
| 2 | 确认 sponsors 表有 avatar 列 | 做 L3 回归验证前 | 查 `information_schema.columns`；无列则 `ALTER TABLE sponsors ADD COLUMN IF NOT EXISTS avatar text;` |
| 3 | M1 补偿回滚回归验证 | 下次审核投稿时顺带 | ① 正常通过一笔，行为不变；② 断网制造失败，投稿回待审核列表 |
| 4 | Worker 重部署确认 | — | S2/M5 + 署名时间戳改动都在 open-api.js，需手动重部署后过回归清单 1/5 |
| 5 | 历史 SQL 文档补废弃标注（M4） | ✅ 已完成（2026-08-28） | 7 个被取代的历史文档头部已统一标注，指向权威版本 |

> M2 已于 2026-08-28 执行并验证通过（[sql/search-recall-two-stage.md](sql/search-recall-two-stage.md)），从待办移除。
