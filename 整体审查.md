已完成深度评估。基于前一轮全量扫描 + 本轮对 SQL 迁移链（`phase2-song-contributors.md` A/B 段、`phase2-batch2-get-artist-songs.md`、`recompute-artist-types.md`、`verify-dual-source.sql`）的交叉核对，以下是结论。

> 说明：本项目**没有 Java 代码**。后端是 SQL（Supabase Postgres）+ JS（Cloudflare Worker、Netlify Function），前端是 Vue3 + TypeScript。下文按「SQL 逻辑 / Vue 业务逻辑 / JS Worker / 异常链路 / 资源释放 / 边界」组织。

---

## 一、业务架构与模块关系

### 1. 分层与调用链

```
消费端(打标工具)         前端                    后端
Lyrico/MusicTag ──► Cloudflare Worker ──► Supabase PostgREST/RPC ─┐
                                        (anon key, RLS)          │
前台 View ──► useSSGData ──► api.ts ────────────────────────────┤
后台 View ──► adminApi.ts ──► supabase(auth) ───────────────────┤
后台写链路 ──► contribRelations/artistTypes ──► RPC/CRUD ────────┤
投稿 ──► submitSubmissionV2 ──► submissions 表                    │
审核 ──► publishSubmission(事务链) ──► 多表顺序写               │
通知 ──► Netlify mailer.mjs ──► Supabase settings(service_role) ─┘
```

### 2. 核心数据模型（本次评估的重心）

- **中间表化**：`song_contributors` / `album_contributors` 取代 `songs.artist_ids/lyricist/composer/arranger`、`albums.artist_ids` 旧列。
- **三段式迁移**（设计良好，是理解全部风险的关键）：
  1. **旧列**（v2 遗留）
  2. **A 段双源**：建中间表 + 迁移数据 + 所有 RPC 改为「中间表 ∪ 旧列」UNION 去重（`song_performer_ids`/`song_artist_ids` 辅助函数）
  3. **B 段单源**：删旧列 + RPC 只读中间表
- **口令拆表**：`song_secrets` + `verify_hidden_unlock_code` RPC（口令明文不下发前端）。

### 3. 设计取舍的客观评价

**优点（做得对的地方）**：
- **SSG + initialState 水合**：`useSSGData` 一次预取、序列化进 HTML、客户端零二次请求，是静态托管（GitHub Pages）下兼顾 SEO 与性能的正确方案。
- **复杂计算下沉 PG**：搜索、作品聚合、类型重算都做成 RPC，避免把全表拉回浏览器算——方向正确。
- **安全分层认真**：RLS + 列级收权（`unlock_code` 排除）+ 口令拆表 + `verify` RPC + FK（`ON DELETE CASCADE`/`RESTRICT`）双兜底。这是 v2 踩坑后沉淀的，质量明显高于一般个人项目。
- **双源过渡的巧思**：A 段让「无论新旧哪版代码在写，查询都正确」，平滑了"先建表后改代码"的部署窗口。

**代价（当前设计的内在约束）**：
- **双源过渡态本身就是负债**：同一语义存在「旧列 / 双源 RPC / 单源 RPC」三个版本，状态不可见，已经产生了真实遗漏（见 S3）。
- **审核发布链用客户端 JS 串行写多表，无事务**——这是**受托管形态约束的必然妥协**：纯静态托管 + Supabase 直连，没有自有 Node 服务端承载事务。要原子化只能下沉为 Supabase Edge Function 或 DB 函数，属于方案 B，不是当前能随手改的。
- **三套平行的数据组装逻辑**：`api.ts`、`adminApi`（各页面）、`Worker` 各自维护一份「中间表 → 展示字段」的映射与 select 常量，改表结构要同步三处，且已经出现不一致（S2 的字段用错正是此后果）。

**现实约束（不空谈）**：单/小团队维护；SQL 靠 markdown 手册手工执行（无 migration runner）；开放 API 回源 anon key 走 PostgREST。任何"引入 ORM/服务端事务层/微服务"的建议都不符合当前部署形态，成本远大于收益。

---

## 二、问题分级

### 【严重风险】——必须尽快处理

| #      | 问题                                                         | 实际影响                                                     | 工作量                                                       |
| ------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **S1** | `netlify/functions/mailer.mjs:52-101` 的 `generateEmailHTML` 对 `user_name/song_title/reject_reason` **不转义直接拼进 HTML**；`notify` 的 `user_name` 来自匿名投稿者 | 恶意投稿者可向**站长邮箱**注入 HTML/钓鱼链接（对比 `generateBatchEmailHTML` 已做 escapeHtml，说明是遗漏不是设计） | 极小：抽一个 escapeHtml 统一包裹 3 个字段，<30 行            |
| **S2** | `cloudflare/open-api.js:557` `handleAlbums` 用 `a.artist_ids` 取专辑艺术家，但 `ALBUM_SELECT`(49 行) 返回的是 `album_contributors` 嵌入 | `/v1/albums` 列表的 `artists` **恒为空数组**，开放 API 对所有打标工具输出错误数据（`/v1/album/:id` 正确用了 `albumArtistIdsOf`，形成对比） | 1 行：`flatMap(a => a.artist_ids \|\| [])` → `flatMap(albumArtistIdsOf)` |
| **S3** | **B 段迁移遗漏**：`get_artist_songs` RPC（定义于 `phase2-batch2-get-artist-songs.md`）仍读旧列 `s.artist_ids/s.lyricist/composer/arranger`（43-72 行），而 `phase2-song-contributors.md` 的 B 段删旧列清单（691-707、709-872）**没有对它单源化** | 一旦执行 B 段删旧列，前台**艺术家作品页（ArtistView）会 500**（RPC 引用了已删除的列）。若 B 段尚未执行，这是执行前的定时炸弹；若已执行，当前已坏 | 小：补一个 `get_artist_songs` 单源化版本（`roles` 与 WHERE 都只读 `song_contributors`），并同步更新文档 |

### 【一般隐患】——建议近期处理

| #      | 问题                                                         | 实际影响                                                     | 工作量                                                      |
| ------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------------------- |
| **M1** | `SubmissionsView.vue:943-1144` 发布链无事务：先置 `status=approved` 再顺序建贡献者/专辑/歌曲/中间表，任一步失败留「已通过但歌曲未建成」中间态 | 失败只能靠人工撤回兜底，审核员可能误以为发布成功             | 中：方案 A 加补偿回滚；彻底解决需下沉 RPC（方案 B）         |
| **M2** | 歌词/标题/歌手名搜索走 `ilike '%kw%'` 全表扫描（`api.ts:344`、`open-api.js:410`），无 `pg_trgm` 索引，前端每 300ms 防抖触发一次 | 数据量上来后搜索变慢、压 PG                                  | 小：建 `pg_trgm` GIN 索引（低风险、可回滚）                 |
| **M3** | 后台新建艺术家 ID 只做非空校验（`SongsView.vue:490-506`、`ArtistInlineForm.vue:99-104`），撞已有 ID 直接 PK 冲突抛原生错误 | 审核/录歌时填重复 ID 报错不友好，且无预检                    | 小：插入前 `in(id)` 查重 + 友好提示                         |
| **M4** | SQL 迁移文档/RPC **版本漂移**：`sql/` 下同一函数有多个历史版本（`search-songs-artist-match.md` 单源旧列版 vs `phase2-song-contributors.md` 双源/单源版），无「线上权威版本」标记 | 后人按旧文档重跑会把 RPC 回退到旧列单源版（B 段删列后直接报列不存在） | 小：给每个 RPC 文档加「当前线上版本」标注，旧版加「已废弃」 |
| **M5** | `cloudflare/open-api.js:85-110` `html404` 把 Host 头未转义插进 HTML | 反射型注入（仅 404 页、只反射给请求者，低危）                | 小：escapeHtml                                              |

### 【优化建议】——锦上添花

| #    | 问题                                                         | 影响                       | 工作量     |
| ---- | ------------------------------------------------------------ | -------------------------- | ---------- |
| L1   | 常量/工具重复：Logo URL 硬编码 4 处、`GENRE_OPTIONS`/`TIP_ICONS`/`langColor`/`copyText`/`VALIDATION_ABORT`/二维码弹窗各 2-3 份 | 改一处漏多处               | 小，纯提纯 |
| L2   | `SubmissionsView` `displayList` O(n·m) 重复 filter（597/624 行）；`recallSubmissions` 每行串行 5-6 次查询；`SettingsView.save` 串行 6 upsert | 大批量/大列表时慢          | 小-中      |
| L3   | `SponsorsView.vue` 头像字段悬空：表单有输入、模板展示 `row.avatar`，但 `save()` payload 不含 `avatar`，`Sponsor` 类型也无此字段 | 保存头像无效               | 小         |
| L4   | `BatchSubmitPanel.buildSongData:405-409` 输出旧列风格裸键（`artist/lyricist/...` 拼接串），单曲路径不输出，与 phase2「清裸键」目标相悖 | 数据不一致，审核端兼容负担 | 小         |
| L5   | 死代码：`legacyCommentsApi`（api.ts:563）、`Comment` 类型、`markReject` 空 if 块 | 阅读噪声                   | 小         |

---

## 三、重点领域的专项结论

### SQL 逻辑
- **双源正确性**：`song_performer_ids`/`song_artist_ids`/`get_artist_songs` 的 `roles` 都用 `UNION`/`DISTINCT` 去重，语义正确。
- **`recompute_artist_types` 覆盖式 + `array_agg(distinct)`**：逻辑正确，幂等；但它的正确性**依赖 A 段双源完整**——若将来有 RPC 漏双源，覆盖式会静默清空类型（这正是 S3 同类风险的放大版）。
- **索引缺口**：`song_contributors`/`album_contributors` 已建 artist_id/song_id 索引；但 `lrc_text/lyrics_text/title/name` 无 trgm 索引（对应 M2）。
- **最需要修的是 S3**：B 段删列清单缺 `get_artist_songs`，这是本次评估在 SQL 层找到的最高价值问题。

### Vue 业务逻辑
- 发布链、审核、撤回、批量投稿的整体流程设计是清晰的（批次 ID 沿用、`published_refs` 级联回收、FK 兜底），主要短板是 **M1 无事务** 和 **L2 串行/重复扫描**。
- 边界处理细致：隐藏歌 SSG 不落歌词、解锁后客户端重拉、全局口令/独立口令分流、空态/未知兜底都到位。

### 异常链路
- `useSSGData` 有 try/catch + `finally loading=false`；各 view 的 `load()` 均有 catch + `ElMessage.error`。
- 薄弱点在 **M1**（发布链异常后状态不回滚）和 `notifyAdminNewSubmission`/邮件 `catch(()=>{})`（静默失败，已注释说明是刻意设计，可接受）。

### 资源释放
- **整体良好**：事件监听（SongView/SearchOverlay/ImgPreview/RewardModal/AdminLayout/ArtistTagInput/ArtistView）、定时器（useBusuanzi/SubmitView/ContactIcons/ArtistView）、Observer（TwikooComment）均有 `onUnmounted`/`clearTimeout` 清理。
- 唯一小瑕疵：`SongView.vue:485` 的 `setTimeout(send, 1500)` 未保存句柄、卸载时不清理——但只是向已卸载 iframe 发 postMessage，无害，可忽略。

### 边界场景
- 已覆盖：空数据、无专辑、无歌词、隐藏歌、口令错误、批次失败重试、撤回回收。
- 缺口：S2（Worker 专辑艺术家）、S3（B 段后 get_artist_songs）、L3（赞助头像）、M3（ID 重复）。

---

## 四、优化方案

### 方案 A：小改动渐进（快速落地，不动架构）

**原则**：只修风险点 + 抽公共代码，每个改动独立可回滚，不碰数据模型与迁移状态。

按顺序执行（每条都是独立 PR）：

1. **修 S1**：mailer.mjs 统一 `escapeHtml` 包裹 `user_name/song_title/reject_reason`。
2. **修 S2**：Worker `handleAlbums` 字段名一行修复。
3. **处理 S3**：先确认线上 B 段是否已执行；无论与否，补 `get_artist_songs` 单源化版本（含 `roles` 只读 `song_contributors`），作为 B 段执行的前置条件。
4. **M2**：建 `pg_trgm` GIN 索引（`songs.lrc_text/lyrics_text/title`、`artists.name`、`albums.name`）。
5. **M3**：后台新建艺术家插入前 `in(id)` 查重。
6. **M5**：Worker `html404` 转义 Host。
7. **L1**：抽 `constants.ts`（Logo/GENRE_OPTIONS/TIP_ICONS/langColor/QR）、`lib/clipboard.ts`、`validateEmail()`、共享 `VALIDATION_ABORT`。
8. **M1 的最小改法**：`publishSubmission` 失败时回写 `status='pending'` + 保留 `published_refs`（已建的部分产物可被撤回链回收），让失败不残留"已通过"假象。
9. **L3/L4/L5**：赞助头像字段、删遗留裸键、清死代码。

**落地成本**：约 1-2 个工作日，绝大部分是低风险小改动，随时可分批上线。

### 方案 B：中长期重构（解决结构性问题）

**1. 完成中间表迁移收尾（消除双源）**
- 补全 B 段清单（含 `get_artist_songs` 单源化）→ 选低峰窗口执行删旧列 → 全链路回归 → 删除旧版本文档/标记权威版本。
- **代价**：删列不可逆，需先备份 + 全 RPC 冒烟；**风险**：若还有遗漏 RPC（类似 S3）会直接线上 500，故必须先做「RPC 全量盘点清单」。

**2. 发布链原子化（治 M1 根）**
- 把 `publishSubmission` 的「建艺术家/贡献者/专辑/歌曲/中间表」客户端串行写，下沉为单个 Supabase RPC（`publish_submission`，`SECURITY DEFINER` + 严格 `set search_path` + 入参白名单校验），单事务内完成，失败整体回滚，`published_refs` 由 DB 生成。
- **代价**：重写审核链路前后端 + 安全审计 + 回归测试，成本高；**风险**：`SECURITY DEFINER` 函数若入参校验不严会成为提权面，需谨慎。

**3. 收敛三套数据组装**
- 让 RPC 出参统一为前端/Worker 直接可用 shape，`api.ts`/`adminApi`/`Worker` 共用同一份 select 常量与「中间表 → 字段」映射，消除 S2 这类"三处语义不同步"。
- **代价**：动 `api.ts`（22KB）+ `adminApi` + Worker 三处，回归面大。

**4. 后台模块拆分 + 迁移工具化**
- `SubmissionsView`（1750 行）拆为 list / review-dialog / batch-review / service。
- 引入 SQL migration 管理（编号文件 + 执行记录），取代 markdown 手工手册，根治 M4 版本漂移。
- **代价**：中，纯工程收益，无业务风险。

**方案 B 的总体代价与风险**：需 1-2 周 + 一次性停写窗口（删列）+ 全链路回归；最大风险集中在「删旧列」和「SECURITY DEFINER RPC」两处，必须配套盘点清单和安全评审，不能图快。

---

## 五、必须改 vs 锦上添花（结论）

**必须改（4 项，优先级从高到低）**：
1. **S1 邮件 HTML 注入**——无条件存在、直接针对站长，且修复成本几乎为零。
2. **S2 Worker 专辑艺术家名恒为空**——开放 API 对外功能缺陷，一行修复。
3. **S3 get_artist_songs 单源化遗漏**——B 段删列的定时炸弹，不修则迁移收尾必炸。
4. **M1 发布链中间态**——影响审核数据正确性，至少先用方案 A 的补偿回滚兜住。

**锦上添花**：其余 L 类（冗余抽取、性能优化、字段悬空、死代码）都不影响正确性，可按人力随版本顺带清理，不必单独排期。

总体判断：这是一个**架构方向正确、迁移策略有巧思、但迁移收尾存在遗漏**的项目。当前最值得投入的，不是重构，而是**把三段式迁移干净地收尾**（补 S3 + 建权威版本清单），以及**补上几个近乎零成本的安全/正确性修复**（S1/S2）。